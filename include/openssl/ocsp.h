/*
 * WARNING: do not edit!
 * Generated by Makefile from include/openssl/ocsp.h.in
 *
 * Copyright 2000-2021 The OpenSSL Project Authors. All Rights Reserved.
 *
 * Licensed under the Apache License 2.0 (the "License").  You may not use
 * this file except in compliance with the License.  You can obtain a copy
 * in the file LICENSE in the source distribution or at
 * https://www.openssl.org/source/license.html
 */

/* clang-format off */

/* clang-format on */

#ifndef OPENSSL_OCSP_H
#define OPENSSL_OCSP_H
#pragma once

#include <openssl/macros.h>
#ifndef OPENSSL_NO_DEPRECATED_3_0
#define HEADER_OCSP_H
#endif

#include <openssl/opensslconf.h>
#include <openssl/http.h>
#include <openssl/asn1.h>

/*
 * These definitions are outside the OPENSSL_NO_OCSP guard because although for
 * historical reasons they have OCSP_* names, they can actually be used
 * independently of OCSP. E.g. see RFC5280
 */
/*-
 *   CRLReason ::= ENUMERATED {
 *        unspecified             (0),
 *        keyCompromise           (1),
 *        cACompromise            (2),
 *        affiliationChanged      (3),
 *        superseded              (4),
 *        cessationOfOperation    (5),
 *        certificateHold         (6),
 *        -- value 7 is not used
 *        removeFromCRL           (8),
 *        privilegeWithdrawn      (9),
 *        aACompromise           (10) }
 */
#define OCSP_REVOKED_STATUS_NOSTATUS -1
#define OCSP_REVOKED_STATUS_UNSPECIFIED 0
#define OCSP_REVOKED_STATUS_KEYCOMPROMISE 1
#define OCSP_REVOKED_STATUS_CACOMPROMISE 2
#define OCSP_REVOKED_STATUS_AFFILIATIONCHANGED 3
#define OCSP_REVOKED_STATUS_SUPERSEDED 4
#define OCSP_REVOKED_STATUS_CESSATIONOFOPERATION 5
#define OCSP_REVOKED_STATUS_CERTIFICATEHOLD 6
#define OCSP_REVOKED_STATUS_REMOVEFROMCRL 8
#define OCSP_REVOKED_STATUS_PRIVILEGEWITHDRAWN 9
#define OCSP_REVOKED_STATUS_AACOMPROMISE 10

#ifndef OPENSSL_NO_OCSP

#include <openssl/x509.h>
#include <openssl/x509v3.h>
#include <openssl/safestack.h>
#include <openssl/ocsperr.h>

#ifdef __cplusplus
extern "C" {
#endif

/* Various flags and values */

#define OCSP_DEFAULT_NONCE_LENGTH 16

#define OCSP_NOCERTS 0x1
#define OCSP_NOINTERN 0x2
#define OCSP_NOSIGS 0x4
#define OCSP_NOCHAIN 0x8
#define OCSP_NOVERIFY 0x10
#define OCSP_NOEXPLICIT 0x20
#define OCSP_NOCASIGN 0x40
#define OCSP_NODELEGATED 0x80
#define OCSP_NOCHECKS 0x100
#define OCSP_TRUSTOTHER 0x200
#define OCSP_RESPID_KEY 0x400
#define OCSP_NOTIME 0x800
#define OCSP_PARTIAL_CHAIN 0x1000

/**
 * @brief OCSP CertID identifying a certificate by issuer name/key hashes and serial (RFC 6960).
 */
struct ocsp_cert_id_st;
/**
 * @brief OCSP CertID identifying a certificate by issuer name/key hashes and serial (RFC 6960).
 */
typedef struct ocsp_cert_id_st OCSP_CERTID;
/**
 * @brief Single OCSP request entry with a CertID and optional extensions (RFC 6960).
 */
struct ocsp_one_request_st;
/**
 * @brief Single OCSP request entry with a CertID and optional extensions (RFC 6960).
 */
typedef struct ocsp_one_request_st OCSP_ONEREQ;
/**
 * @brief OCSP TBSRequest: version, optional requestor name, request list, and extensions.
 */
struct ocsp_req_info_st;
/**
 * @brief OCSP TBSRequest: version, optional requestor name, request list, and extensions.
 */
typedef struct ocsp_req_info_st OCSP_REQINFO;
/**
 * @brief Optional signature over an OCSP request, with algorithm and certificates.
 */
struct ocsp_signature_st;
/**
 * @brief Optional signature over an OCSP request, with algorithm and certificates.
 */
typedef struct ocsp_signature_st OCSP_SIGNATURE;
/**
 * @brief OCSP Request message containing TBSRequest and optional signature (RFC 6960).
 */
struct ocsp_request_st;
/**
 * @brief OCSP Request message containing TBSRequest and optional signature (RFC 6960).
 */
typedef struct ocsp_request_st OCSP_REQUEST;

/* clang-format off */
SKM_DEFINE_STACK_OF_INTERNAL(OCSP_CERTID, OCSP_CERTID, OCSP_CERTID)
#define sk_OCSP_CERTID_num(sk) OPENSSL_sk_num(ossl_check_const_OCSP_CERTID_sk_type(sk))
#define sk_OCSP_CERTID_value(sk, idx) ((OCSP_CERTID *)OPENSSL_sk_value(ossl_check_const_OCSP_CERTID_sk_type(sk), (idx)))
#define sk_OCSP_CERTID_new(cmp) ((STACK_OF(OCSP_CERTID) *)OPENSSL_sk_new(ossl_check_OCSP_CERTID_compfunc_type(cmp)))
#define sk_OCSP_CERTID_new_null() ((STACK_OF(OCSP_CERTID) *)OPENSSL_sk_new_null())
#define sk_OCSP_CERTID_new_reserve(cmp, n) ((STACK_OF(OCSP_CERTID) *)OPENSSL_sk_new_reserve(ossl_check_OCSP_CERTID_compfunc_type(cmp), (n)))
#define sk_OCSP_CERTID_reserve(sk, n) OPENSSL_sk_reserve(ossl_check_OCSP_CERTID_sk_type(sk), (n))
#define sk_OCSP_CERTID_free(sk) OPENSSL_sk_free(ossl_check_OCSP_CERTID_sk_type(sk))
#define sk_OCSP_CERTID_zero(sk) OPENSSL_sk_zero(ossl_check_OCSP_CERTID_sk_type(sk))
#define sk_OCSP_CERTID_delete(sk, i) ((OCSP_CERTID *)OPENSSL_sk_delete(ossl_check_OCSP_CERTID_sk_type(sk), (i)))
#define sk_OCSP_CERTID_delete_ptr(sk, ptr) ((OCSP_CERTID *)OPENSSL_sk_delete_ptr(ossl_check_OCSP_CERTID_sk_type(sk), ossl_check_OCSP_CERTID_type(ptr)))
#define sk_OCSP_CERTID_push(sk, ptr) OPENSSL_sk_push(ossl_check_OCSP_CERTID_sk_type(sk), ossl_check_OCSP_CERTID_type(ptr))
#define sk_OCSP_CERTID_unshift(sk, ptr) OPENSSL_sk_unshift(ossl_check_OCSP_CERTID_sk_type(sk), ossl_check_OCSP_CERTID_type(ptr))
#define sk_OCSP_CERTID_pop(sk) ((OCSP_CERTID *)OPENSSL_sk_pop(ossl_check_OCSP_CERTID_sk_type(sk)))
#define sk_OCSP_CERTID_shift(sk) ((OCSP_CERTID *)OPENSSL_sk_shift(ossl_check_OCSP_CERTID_sk_type(sk)))
#define sk_OCSP_CERTID_pop_free(sk, freefunc) OPENSSL_sk_pop_free(ossl_check_OCSP_CERTID_sk_type(sk),ossl_check_OCSP_CERTID_freefunc_type(freefunc))
#define sk_OCSP_CERTID_insert(sk, ptr, idx) OPENSSL_sk_insert(ossl_check_OCSP_CERTID_sk_type(sk), ossl_check_OCSP_CERTID_type(ptr), (idx))
#define sk_OCSP_CERTID_set(sk, idx, ptr) ((OCSP_CERTID *)OPENSSL_sk_set(ossl_check_OCSP_CERTID_sk_type(sk), (idx), ossl_check_OCSP_CERTID_type(ptr)))
#define sk_OCSP_CERTID_find(sk, ptr) OPENSSL_sk_find(ossl_check_OCSP_CERTID_sk_type(sk), ossl_check_OCSP_CERTID_type(ptr))
#define sk_OCSP_CERTID_find_ex(sk, ptr) OPENSSL_sk_find_ex(ossl_check_OCSP_CERTID_sk_type(sk), ossl_check_OCSP_CERTID_type(ptr))
#define sk_OCSP_CERTID_find_all(sk, ptr, pnum) OPENSSL_sk_find_all(ossl_check_OCSP_CERTID_sk_type(sk), ossl_check_OCSP_CERTID_type(ptr), pnum)
#define sk_OCSP_CERTID_sort(sk) OPENSSL_sk_sort(ossl_check_OCSP_CERTID_sk_type(sk))
#define sk_OCSP_CERTID_is_sorted(sk) OPENSSL_sk_is_sorted(ossl_check_const_OCSP_CERTID_sk_type(sk))
#define sk_OCSP_CERTID_dup(sk) ((STACK_OF(OCSP_CERTID) *)OPENSSL_sk_dup(ossl_check_const_OCSP_CERTID_sk_type(sk)))
#define sk_OCSP_CERTID_deep_copy(sk, copyfunc, freefunc) ((STACK_OF(OCSP_CERTID) *)OPENSSL_sk_deep_copy(ossl_check_const_OCSP_CERTID_sk_type(sk), ossl_check_OCSP_CERTID_copyfunc_type(copyfunc), ossl_check_OCSP_CERTID_freefunc_type(freefunc)))
#define sk_OCSP_CERTID_set_cmp_func(sk, cmp) ((sk_OCSP_CERTID_compfunc)OPENSSL_sk_set_cmp_func(ossl_check_OCSP_CERTID_sk_type(sk), ossl_check_OCSP_CERTID_compfunc_type(cmp)))
SKM_DEFINE_STACK_OF_INTERNAL(OCSP_ONEREQ, OCSP_ONEREQ, OCSP_ONEREQ)
#define sk_OCSP_ONEREQ_num(sk) OPENSSL_sk_num(ossl_check_const_OCSP_ONEREQ_sk_type(sk))
#define sk_OCSP_ONEREQ_value(sk, idx) ((OCSP_ONEREQ *)OPENSSL_sk_value(ossl_check_const_OCSP_ONEREQ_sk_type(sk), (idx)))
#define sk_OCSP_ONEREQ_new(cmp) ((STACK_OF(OCSP_ONEREQ) *)OPENSSL_sk_new(ossl_check_OCSP_ONEREQ_compfunc_type(cmp)))
#define sk_OCSP_ONEREQ_new_null() ((STACK_OF(OCSP_ONEREQ) *)OPENSSL_sk_new_null())
#define sk_OCSP_ONEREQ_new_reserve(cmp, n) ((STACK_OF(OCSP_ONEREQ) *)OPENSSL_sk_new_reserve(ossl_check_OCSP_ONEREQ_compfunc_type(cmp), (n)))
#define sk_OCSP_ONEREQ_reserve(sk, n) OPENSSL_sk_reserve(ossl_check_OCSP_ONEREQ_sk_type(sk), (n))
#define sk_OCSP_ONEREQ_free(sk) OPENSSL_sk_free(ossl_check_OCSP_ONEREQ_sk_type(sk))
#define sk_OCSP_ONEREQ_zero(sk) OPENSSL_sk_zero(ossl_check_OCSP_ONEREQ_sk_type(sk))
#define sk_OCSP_ONEREQ_delete(sk, i) ((OCSP_ONEREQ *)OPENSSL_sk_delete(ossl_check_OCSP_ONEREQ_sk_type(sk), (i)))
#define sk_OCSP_ONEREQ_delete_ptr(sk, ptr) ((OCSP_ONEREQ *)OPENSSL_sk_delete_ptr(ossl_check_OCSP_ONEREQ_sk_type(sk), ossl_check_OCSP_ONEREQ_type(ptr)))
#define sk_OCSP_ONEREQ_push(sk, ptr) OPENSSL_sk_push(ossl_check_OCSP_ONEREQ_sk_type(sk), ossl_check_OCSP_ONEREQ_type(ptr))
#define sk_OCSP_ONEREQ_unshift(sk, ptr) OPENSSL_sk_unshift(ossl_check_OCSP_ONEREQ_sk_type(sk), ossl_check_OCSP_ONEREQ_type(ptr))
#define sk_OCSP_ONEREQ_pop(sk) ((OCSP_ONEREQ *)OPENSSL_sk_pop(ossl_check_OCSP_ONEREQ_sk_type(sk)))
#define sk_OCSP_ONEREQ_shift(sk) ((OCSP_ONEREQ *)OPENSSL_sk_shift(ossl_check_OCSP_ONEREQ_sk_type(sk)))
#define sk_OCSP_ONEREQ_pop_free(sk, freefunc) OPENSSL_sk_pop_free(ossl_check_OCSP_ONEREQ_sk_type(sk),ossl_check_OCSP_ONEREQ_freefunc_type(freefunc))
#define sk_OCSP_ONEREQ_insert(sk, ptr, idx) OPENSSL_sk_insert(ossl_check_OCSP_ONEREQ_sk_type(sk), ossl_check_OCSP_ONEREQ_type(ptr), (idx))
#define sk_OCSP_ONEREQ_set(sk, idx, ptr) ((OCSP_ONEREQ *)OPENSSL_sk_set(ossl_check_OCSP_ONEREQ_sk_type(sk), (idx), ossl_check_OCSP_ONEREQ_type(ptr)))
#define sk_OCSP_ONEREQ_find(sk, ptr) OPENSSL_sk_find(ossl_check_OCSP_ONEREQ_sk_type(sk), ossl_check_OCSP_ONEREQ_type(ptr))
#define sk_OCSP_ONEREQ_find_ex(sk, ptr) OPENSSL_sk_find_ex(ossl_check_OCSP_ONEREQ_sk_type(sk), ossl_check_OCSP_ONEREQ_type(ptr))
#define sk_OCSP_ONEREQ_find_all(sk, ptr, pnum) OPENSSL_sk_find_all(ossl_check_OCSP_ONEREQ_sk_type(sk), ossl_check_OCSP_ONEREQ_type(ptr), pnum)
#define sk_OCSP_ONEREQ_sort(sk) OPENSSL_sk_sort(ossl_check_OCSP_ONEREQ_sk_type(sk))
#define sk_OCSP_ONEREQ_is_sorted(sk) OPENSSL_sk_is_sorted(ossl_check_const_OCSP_ONEREQ_sk_type(sk))
#define sk_OCSP_ONEREQ_dup(sk) ((STACK_OF(OCSP_ONEREQ) *)OPENSSL_sk_dup(ossl_check_const_OCSP_ONEREQ_sk_type(sk)))
#define sk_OCSP_ONEREQ_deep_copy(sk, copyfunc, freefunc) ((STACK_OF(OCSP_ONEREQ) *)OPENSSL_sk_deep_copy(ossl_check_const_OCSP_ONEREQ_sk_type(sk), ossl_check_OCSP_ONEREQ_copyfunc_type(copyfunc), ossl_check_OCSP_ONEREQ_freefunc_type(freefunc)))
#define sk_OCSP_ONEREQ_set_cmp_func(sk, cmp) ((sk_OCSP_ONEREQ_compfunc)OPENSSL_sk_set_cmp_func(ossl_check_OCSP_ONEREQ_sk_type(sk), ossl_check_OCSP_ONEREQ_compfunc_type(cmp)))

/* clang-format on */

#define OCSP_RESPONSE_STATUS_SUCCESSFUL 0
#define OCSP_RESPONSE_STATUS_MALFORMEDREQUEST 1
#define OCSP_RESPONSE_STATUS_INTERNALERROR 2
#define OCSP_RESPONSE_STATUS_TRYLATER 3
#define OCSP_RESPONSE_STATUS_SIGREQUIRED 5
#define OCSP_RESPONSE_STATUS_UNAUTHORIZED 6

/**
 * @brief OCSP ResponseBytes: responseType OID and response OCTET STRING payload.
 */
struct ocsp_resp_bytes_st;
/**
 * @brief OCSP ResponseBytes: responseType OID and response OCTET STRING payload.
 */
typedef struct ocsp_resp_bytes_st OCSP_RESPBYTES;

#define V_OCSP_RESPID_NAME 0
#define V_OCSP_RESPID_KEY 1

/* clang-format off */
SKM_DEFINE_STACK_OF_INTERNAL(OCSP_RESPID, OCSP_RESPID, OCSP_RESPID)
#define sk_OCSP_RESPID_num(sk) OPENSSL_sk_num(ossl_check_const_OCSP_RESPID_sk_type(sk))
#define sk_OCSP_RESPID_value(sk, idx) ((OCSP_RESPID *)OPENSSL_sk_value(ossl_check_const_OCSP_RESPID_sk_type(sk), (idx)))
#define sk_OCSP_RESPID_new(cmp) ((STACK_OF(OCSP_RESPID) *)OPENSSL_sk_new(ossl_check_OCSP_RESPID_compfunc_type(cmp)))
#define sk_OCSP_RESPID_new_null() ((STACK_OF(OCSP_RESPID) *)OPENSSL_sk_new_null())
#define sk_OCSP_RESPID_new_reserve(cmp, n) ((STACK_OF(OCSP_RESPID) *)OPENSSL_sk_new_reserve(ossl_check_OCSP_RESPID_compfunc_type(cmp), (n)))
#define sk_OCSP_RESPID_reserve(sk, n) OPENSSL_sk_reserve(ossl_check_OCSP_RESPID_sk_type(sk), (n))
#define sk_OCSP_RESPID_free(sk) OPENSSL_sk_free(ossl_check_OCSP_RESPID_sk_type(sk))
#define sk_OCSP_RESPID_zero(sk) OPENSSL_sk_zero(ossl_check_OCSP_RESPID_sk_type(sk))
#define sk_OCSP_RESPID_delete(sk, i) ((OCSP_RESPID *)OPENSSL_sk_delete(ossl_check_OCSP_RESPID_sk_type(sk), (i)))
#define sk_OCSP_RESPID_delete_ptr(sk, ptr) ((OCSP_RESPID *)OPENSSL_sk_delete_ptr(ossl_check_OCSP_RESPID_sk_type(sk), ossl_check_OCSP_RESPID_type(ptr)))
#define sk_OCSP_RESPID_push(sk, ptr) OPENSSL_sk_push(ossl_check_OCSP_RESPID_sk_type(sk), ossl_check_OCSP_RESPID_type(ptr))
#define sk_OCSP_RESPID_unshift(sk, ptr) OPENSSL_sk_unshift(ossl_check_OCSP_RESPID_sk_type(sk), ossl_check_OCSP_RESPID_type(ptr))
#define sk_OCSP_RESPID_pop(sk) ((OCSP_RESPID *)OPENSSL_sk_pop(ossl_check_OCSP_RESPID_sk_type(sk)))
#define sk_OCSP_RESPID_shift(sk) ((OCSP_RESPID *)OPENSSL_sk_shift(ossl_check_OCSP_RESPID_sk_type(sk)))
#define sk_OCSP_RESPID_pop_free(sk, freefunc) OPENSSL_sk_pop_free(ossl_check_OCSP_RESPID_sk_type(sk),ossl_check_OCSP_RESPID_freefunc_type(freefunc))
#define sk_OCSP_RESPID_insert(sk, ptr, idx) OPENSSL_sk_insert(ossl_check_OCSP_RESPID_sk_type(sk), ossl_check_OCSP_RESPID_type(ptr), (idx))
#define sk_OCSP_RESPID_set(sk, idx, ptr) ((OCSP_RESPID *)OPENSSL_sk_set(ossl_check_OCSP_RESPID_sk_type(sk), (idx), ossl_check_OCSP_RESPID_type(ptr)))
#define sk_OCSP_RESPID_find(sk, ptr) OPENSSL_sk_find(ossl_check_OCSP_RESPID_sk_type(sk), ossl_check_OCSP_RESPID_type(ptr))
#define sk_OCSP_RESPID_find_ex(sk, ptr) OPENSSL_sk_find_ex(ossl_check_OCSP_RESPID_sk_type(sk), ossl_check_OCSP_RESPID_type(ptr))
#define sk_OCSP_RESPID_find_all(sk, ptr, pnum) OPENSSL_sk_find_all(ossl_check_OCSP_RESPID_sk_type(sk), ossl_check_OCSP_RESPID_type(ptr), pnum)
#define sk_OCSP_RESPID_sort(sk) OPENSSL_sk_sort(ossl_check_OCSP_RESPID_sk_type(sk))
#define sk_OCSP_RESPID_is_sorted(sk) OPENSSL_sk_is_sorted(ossl_check_const_OCSP_RESPID_sk_type(sk))
#define sk_OCSP_RESPID_dup(sk) ((STACK_OF(OCSP_RESPID) *)OPENSSL_sk_dup(ossl_check_const_OCSP_RESPID_sk_type(sk)))
#define sk_OCSP_RESPID_deep_copy(sk, copyfunc, freefunc) ((STACK_OF(OCSP_RESPID) *)OPENSSL_sk_deep_copy(ossl_check_const_OCSP_RESPID_sk_type(sk), ossl_check_OCSP_RESPID_copyfunc_type(copyfunc), ossl_check_OCSP_RESPID_freefunc_type(freefunc)))
#define sk_OCSP_RESPID_set_cmp_func(sk, cmp) ((sk_OCSP_RESPID_compfunc)OPENSSL_sk_set_cmp_func(ossl_check_OCSP_RESPID_sk_type(sk), ossl_check_OCSP_RESPID_compfunc_type(cmp)))

/* clang-format on */

/**
 * @brief OCSP RevokedInfo: revocation time and optional CRL reason (RFC 6960).
 */
struct ocsp_revoked_info_st;
/**
 * @brief OCSP RevokedInfo: revocation time and optional CRL reason (RFC 6960).
 */
typedef struct ocsp_revoked_info_st OCSP_REVOKEDINFO;

#define V_OCSP_CERTSTATUS_GOOD 0
#define V_OCSP_CERTSTATUS_REVOKED 1
#define V_OCSP_CERTSTATUS_UNKNOWN 2

/**
 * @brief OCSP CertStatus CHOICE: good, revoked, or unknown (RFC 6960).
 */
struct ocsp_cert_status_st;
/**
 * @brief OCSP CertStatus CHOICE: good, revoked, or unknown (RFC 6960).
 */
typedef struct ocsp_cert_status_st OCSP_CERTSTATUS;
/**
 * @brief OCSP SingleResponse for one CertID: status, thisUpdate, nextUpdate, and extensions.
 */
struct ocsp_single_response_st;
/**
 * @brief OCSP SingleResponse for one CertID: status, thisUpdate, nextUpdate, and extensions.
 */
typedef struct ocsp_single_response_st OCSP_SINGLERESP;

/* clang-format off */
SKM_DEFINE_STACK_OF_INTERNAL(OCSP_SINGLERESP, OCSP_SINGLERESP, OCSP_SINGLERESP)
#define sk_OCSP_SINGLERESP_num(sk) OPENSSL_sk_num(ossl_check_const_OCSP_SINGLERESP_sk_type(sk))
#define sk_OCSP_SINGLERESP_value(sk, idx) ((OCSP_SINGLERESP *)OPENSSL_sk_value(ossl_check_const_OCSP_SINGLERESP_sk_type(sk), (idx)))
#define sk_OCSP_SINGLERESP_new(cmp) ((STACK_OF(OCSP_SINGLERESP) *)OPENSSL_sk_new(ossl_check_OCSP_SINGLERESP_compfunc_type(cmp)))
#define sk_OCSP_SINGLERESP_new_null() ((STACK_OF(OCSP_SINGLERESP) *)OPENSSL_sk_new_null())
#define sk_OCSP_SINGLERESP_new_reserve(cmp, n) ((STACK_OF(OCSP_SINGLERESP) *)OPENSSL_sk_new_reserve(ossl_check_OCSP_SINGLERESP_compfunc_type(cmp), (n)))
#define sk_OCSP_SINGLERESP_reserve(sk, n) OPENSSL_sk_reserve(ossl_check_OCSP_SINGLERESP_sk_type(sk), (n))
#define sk_OCSP_SINGLERESP_free(sk) OPENSSL_sk_free(ossl_check_OCSP_SINGLERESP_sk_type(sk))
#define sk_OCSP_SINGLERESP_zero(sk) OPENSSL_sk_zero(ossl_check_OCSP_SINGLERESP_sk_type(sk))
#define sk_OCSP_SINGLERESP_delete(sk, i) ((OCSP_SINGLERESP *)OPENSSL_sk_delete(ossl_check_OCSP_SINGLERESP_sk_type(sk), (i)))
#define sk_OCSP_SINGLERESP_delete_ptr(sk, ptr) ((OCSP_SINGLERESP *)OPENSSL_sk_delete_ptr(ossl_check_OCSP_SINGLERESP_sk_type(sk), ossl_check_OCSP_SINGLERESP_type(ptr)))
#define sk_OCSP_SINGLERESP_push(sk, ptr) OPENSSL_sk_push(ossl_check_OCSP_SINGLERESP_sk_type(sk), ossl_check_OCSP_SINGLERESP_type(ptr))
#define sk_OCSP_SINGLERESP_unshift(sk, ptr) OPENSSL_sk_unshift(ossl_check_OCSP_SINGLERESP_sk_type(sk), ossl_check_OCSP_SINGLERESP_type(ptr))
#define sk_OCSP_SINGLERESP_pop(sk) ((OCSP_SINGLERESP *)OPENSSL_sk_pop(ossl_check_OCSP_SINGLERESP_sk_type(sk)))
#define sk_OCSP_SINGLERESP_shift(sk) ((OCSP_SINGLERESP *)OPENSSL_sk_shift(ossl_check_OCSP_SINGLERESP_sk_type(sk)))
#define sk_OCSP_SINGLERESP_pop_free(sk, freefunc) OPENSSL_sk_pop_free(ossl_check_OCSP_SINGLERESP_sk_type(sk),ossl_check_OCSP_SINGLERESP_freefunc_type(freefunc))
#define sk_OCSP_SINGLERESP_insert(sk, ptr, idx) OPENSSL_sk_insert(ossl_check_OCSP_SINGLERESP_sk_type(sk), ossl_check_OCSP_SINGLERESP_type(ptr), (idx))
#define sk_OCSP_SINGLERESP_set(sk, idx, ptr) ((OCSP_SINGLERESP *)OPENSSL_sk_set(ossl_check_OCSP_SINGLERESP_sk_type(sk), (idx), ossl_check_OCSP_SINGLERESP_type(ptr)))
#define sk_OCSP_SINGLERESP_find(sk, ptr) OPENSSL_sk_find(ossl_check_OCSP_SINGLERESP_sk_type(sk), ossl_check_OCSP_SINGLERESP_type(ptr))
#define sk_OCSP_SINGLERESP_find_ex(sk, ptr) OPENSSL_sk_find_ex(ossl_check_OCSP_SINGLERESP_sk_type(sk), ossl_check_OCSP_SINGLERESP_type(ptr))
#define sk_OCSP_SINGLERESP_find_all(sk, ptr, pnum) OPENSSL_sk_find_all(ossl_check_OCSP_SINGLERESP_sk_type(sk), ossl_check_OCSP_SINGLERESP_type(ptr), pnum)
#define sk_OCSP_SINGLERESP_sort(sk) OPENSSL_sk_sort(ossl_check_OCSP_SINGLERESP_sk_type(sk))
#define sk_OCSP_SINGLERESP_is_sorted(sk) OPENSSL_sk_is_sorted(ossl_check_const_OCSP_SINGLERESP_sk_type(sk))
#define sk_OCSP_SINGLERESP_dup(sk) ((STACK_OF(OCSP_SINGLERESP) *)OPENSSL_sk_dup(ossl_check_const_OCSP_SINGLERESP_sk_type(sk)))
#define sk_OCSP_SINGLERESP_deep_copy(sk, copyfunc, freefunc) ((STACK_OF(OCSP_SINGLERESP) *)OPENSSL_sk_deep_copy(ossl_check_const_OCSP_SINGLERESP_sk_type(sk), ossl_check_OCSP_SINGLERESP_copyfunc_type(copyfunc), ossl_check_OCSP_SINGLERESP_freefunc_type(freefunc)))
#define sk_OCSP_SINGLERESP_set_cmp_func(sk, cmp) ((sk_OCSP_SINGLERESP_compfunc)OPENSSL_sk_set_cmp_func(ossl_check_OCSP_SINGLERESP_sk_type(sk), ossl_check_OCSP_SINGLERESP_compfunc_type(cmp)))

/* clang-format on */

/**
 * @brief OCSP ResponseData (tbsResponseData): version, ResponderID, producedAt, and responses.
 */
struct ocsp_response_data_st;
/**
 * @brief OCSP ResponseData (tbsResponseData): version, ResponderID, producedAt, and responses.
 */
typedef struct ocsp_response_data_st OCSP_RESPDATA;
/**
 * @brief OCSP BasicOCSPResponse: ResponseData with signature and optional certificates.
 */
struct ocsp_basic_response_st;
/**
 * @brief OCSP BasicOCSPResponse: ResponseData with signature and optional certificates.
 */
typedef struct ocsp_basic_response_st OCSP_BASICRESP;
/**
 * @brief OCSP CrlID extension identifying a CRL by URL, number, or time.
 */
struct ocsp_crl_id_st;
/**
 * @brief OCSP CrlID extension identifying a CRL by URL, number, or time.
 */
typedef struct ocsp_crl_id_st OCSP_CRLID;
/**
 * @brief OCSP ServiceLocator extension with issuer name and locator AccessDescriptions.
 */
struct ocsp_service_locator_st;
/**
 * @brief OCSP ServiceLocator extension with issuer name and locator AccessDescriptions.
 */
typedef struct ocsp_service_locator_st OCSP_SERVICELOC;

#define PEM_STRING_OCSP_REQUEST "OCSP REQUEST"
#define PEM_STRING_OCSP_RESPONSE "OCSP RESPONSE"

#define d2i_OCSP_REQUEST_bio(bp, p) ASN1_d2i_bio_of(OCSP_REQUEST, OCSP_REQUEST_new, d2i_OCSP_REQUEST, bp, p)

#define d2i_OCSP_RESPONSE_bio(bp, p) ASN1_d2i_bio_of(OCSP_RESPONSE, OCSP_RESPONSE_new, d2i_OCSP_RESPONSE, bp, p)

#define PEM_read_bio_OCSP_REQUEST(bp, x, cb) (OCSP_REQUEST *)PEM_ASN1_read_bio( \
    (d2i_of_void *)d2i_OCSP_REQUEST, PEM_STRING_OCSP_REQUEST,                   \
    bp, (char **)(x), cb, NULL)

#define PEM_read_bio_OCSP_RESPONSE(bp, x, cb) (OCSP_RESPONSE *)PEM_ASN1_read_bio( \
    (d2i_of_void *)d2i_OCSP_RESPONSE, PEM_STRING_OCSP_RESPONSE,                   \
    bp, (char **)(x), cb, NULL)

#define PEM_write_bio_OCSP_REQUEST(bp, o)                                        \
    PEM_ASN1_write_bio((i2d_of_void *)i2d_OCSP_REQUEST, PEM_STRING_OCSP_REQUEST, \
        bp, (char *)(o), NULL, NULL, 0, NULL, NULL)

#define PEM_write_bio_OCSP_RESPONSE(bp, o)                                         \
    PEM_ASN1_write_bio((i2d_of_void *)i2d_OCSP_RESPONSE, PEM_STRING_OCSP_RESPONSE, \
        bp, (char *)(o), NULL, NULL, 0, NULL, NULL)

#define i2d_OCSP_RESPONSE_bio(bp, o) ASN1_i2d_bio_of(OCSP_RESPONSE, i2d_OCSP_RESPONSE, bp, o)

#define i2d_OCSP_REQUEST_bio(bp, o) ASN1_i2d_bio_of(OCSP_REQUEST, i2d_OCSP_REQUEST, bp, o)

#define ASN1_BIT_STRING_digest(data, type, md, len) \
    ASN1_item_digest(ASN1_ITEM_rptr(ASN1_BIT_STRING), type, data, md, len)

#define OCSP_CERTSTATUS_dup(cs)                                     \
    (OCSP_CERTSTATUS *)ASN1_dup((i2d_of_void *)i2d_OCSP_CERTSTATUS, \
        (d2i_of_void *)d2i_OCSP_CERTSTATUS, (char *)(cs))

/**
 * @brief Duplicate an OCSP CertID structure.
 * @param a CertID to copy.
 * @return Newly allocated copy of @p a, or NULL on error.
 */
OCSP_CERTID *OCSP_CERTID_dup(const OCSP_CERTID *a);

/**
 * @brief Create a non-blocking HTTP request context for an OCSP exchange.
 * @param io BIO connected to the OCSP responder.
 * @param path HTTP request-URI path for the OCSP service (for example "/").
 * @param req Optional OCSP request to send immediately, or NULL to set later.
 * @param buf_size I/O buffer size hint, or 0 for the default.
 * @return New OSSL_HTTP_REQ_CTX, or NULL on error; free with OSSL_HTTP_REQ_CTX_free().
 */
OSSL_HTTP_REQ_CTX *OCSP_sendreq_new(BIO *io, const char *path,
    const OCSP_REQUEST *req, int buf_size);
/**
 * @brief Send an OCSP request over HTTP via @p b and return the decoded response.
 * @param b BIO connected to the OCSP responder (typically a connected socket BIO).
 * @param path HTTP request-URI path for the OCSP service (for example "/").
 * @param req OCSP request to POST as application/ocsp-request.
 * @return Newly allocated OCSP_RESPONSE, or NULL on error; free with OCSP_RESPONSE_free().
 */
OCSP_RESPONSE *OCSP_sendreq_bio(BIO *b, const char *path, OCSP_REQUEST *req);

#ifndef OPENSSL_NO_DEPRECATED_3_0
/**
 * @brief Legacy alias for OSSL_HTTP_REQ_CTX used by older OCSP HTTP client APIs.
 */
typedef OSSL_HTTP_REQ_CTX OCSP_REQ_CTX;
#define OCSP_REQ_CTX_new(io, buf_size) \
    OSSL_HTTP_REQ_CTX_new(io, io, buf_size)
#define OCSP_REQ_CTX_free OSSL_HTTP_REQ_CTX_free
#define OCSP_REQ_CTX_http(rctx, op, path) \
    (OSSL_HTTP_REQ_CTX_set_expected(rctx, NULL, 1 /* asn1 */, 0, 0) && OSSL_HTTP_REQ_CTX_set_request_line(rctx, strcmp(op, "POST") == 0, NULL, NULL, path))
#define OCSP_REQ_CTX_add1_header OSSL_HTTP_REQ_CTX_add1_header
#define OCSP_REQ_CTX_i2d(r, it, req) \
    OSSL_HTTP_REQ_CTX_set1_req(r, "application/ocsp-request", it, req)
#define OCSP_REQ_CTX_set1_req(r, req) \
    OCSP_REQ_CTX_i2d(r, ASN1_ITEM_rptr(OCSP_REQUEST), (ASN1_VALUE *)(req))
#define OCSP_REQ_CTX_nbio OSSL_HTTP_REQ_CTX_nbio
#define OCSP_REQ_CTX_nbio_d2i OSSL_HTTP_REQ_CTX_nbio_d2i
#define OCSP_sendreq_nbio(p, r)                       \
    OSSL_HTTP_REQ_CTX_nbio_d2i(r, (ASN1_VALUE **)(p), \
        ASN1_ITEM_rptr(OCSP_RESPONSE))
#define OCSP_REQ_CTX_get0_mem_bio OSSL_HTTP_REQ_CTX_get0_mem_bio
#define OCSP_set_max_response_length OSSL_HTTP_REQ_CTX_set_max_response_length
#endif

/**
 * @brief Build an OCSP CertID from a subject certificate and its issuer.
 * @param dgst Digest used for name and key hashes, or NULL for SHA-1.
 * @param subject Certificate whose serial and issuer hashes identify the CertID.
 * @param issuer Issuer certificate providing the issuer name and public key.
 * @return New OCSP_CERTID, or NULL on error; free with OCSP_CERTID_free().
 */
OCSP_CERTID *OCSP_cert_to_id(const EVP_MD *dgst, const X509 *subject,
    const X509 *issuer);

/**
 * @brief Build an OCSP CertID from issuer name/key material and a serial number.
 * @param dgst Digest used for name and key hashes, or NULL for SHA-1.
 * @param issuerName Issuer distinguished name whose hash becomes issuerNameHash.
 * @param issuerKey Issuer public key BIT STRING whose hash becomes issuerKeyHash.
 * @param serialNumber Certificate serial number copied into the CertID.
 * @return New OCSP_CERTID, or NULL on error; free with OCSP_CERTID_free().
 */
OCSP_CERTID *OCSP_cert_id_new(const EVP_MD *dgst,
    const X509_NAME *issuerName,
    const ASN1_BIT_STRING *issuerKey,
    const ASN1_INTEGER *serialNumber);

/**
 * @brief Append a CertID as a new single-request entry, taking ownership of @p cid.
 * @param req OCSP request to update.
 * @param cid CertID identifying the certificate to query; ownership transfers on success.
 * @return Internal OCSP_ONEREQ pointer on success, or NULL on error.
 */
OCSP_ONEREQ *OCSP_request_add0_id(OCSP_REQUEST *req, OCSP_CERTID *cid);

/**
 * @brief Add an OCSP nonce extension to a request.
 * @param req OCSP request to update.
 * @param val Nonce bytes to copy, or NULL to generate a random nonce of length @p len (or a default length when @p len <= 0).
 * @param len Length of @p val, or desired generated length when @p val is NULL.
 * @return 1 on success, or 0 on failure.
 */
int OCSP_request_add1_nonce(OCSP_REQUEST *req, unsigned char *val, int len);
/**
 * @brief Add an OCSP nonce extension to a basic response.
 * @param resp Basic OCSP response to update.
 * @param val Nonce bytes to copy, or NULL to generate a random nonce of length @p len (or a default length when @p len <= 0).
 * @param len Length of @p val, or desired generated length when @p val is NULL.
 * @return 1 on success, or 0 on failure.
 */
int OCSP_basic_add1_nonce(OCSP_BASICRESP *resp, unsigned char *val, int len);
/**
 * @brief Compare OCSP nonce extensions between a request and a basic response.
 * @param req OCSP request that may contain a nonce.
 * @param bs Basic response that may contain a matching nonce.
 * @return 1 if nonces match, 2 if neither has a nonce, 3 if only the response has one,
 *         0 if present but unequal, or -1 if only the request has a nonce.
 */
int OCSP_check_nonce(OCSP_REQUEST *req, OCSP_BASICRESP *bs);
/**
 * @brief Copy the nonce extension from an OCSP request into a basic response.
 * @param resp Basic response receiving the nonce.
 * @param req Request whose nonce extension is copied when present.
 * @return 1 if a nonce was copied, 2 if the request had no nonce, or 0 on error.
 */
int OCSP_copy_nonce(OCSP_BASICRESP *resp, OCSP_REQUEST *req);

/**
 * @brief Set the optional requestorName in an OCSP request to a directory name.
 * @param req OCSP request to update.
 * @param nm X.509 name copied into requestorName.
 * @return 1 on success, or 0 on failure.
 */
int OCSP_request_set1_name(OCSP_REQUEST *req, const X509_NAME *nm);
/**
 * @brief Add a certificate to the optional certs field of a signed OCSP request.
 * @param req OCSP request to update (typically after signing setup).
 * @param cert Certificate to include; its reference count is incremented.
 * @return 1 on success, or 0 on failure.
 */
int OCSP_request_add1_cert(OCSP_REQUEST *req, X509 *cert);

/**
 * @brief Sign an OCSP request with @p signer and @p key.
 * @param req Request whose TBSRequest is signed.
 * @param signer Certificate identifying the requestor.
 * @param key Private key corresponding to @p signer.
 * @param dgst Digest algorithm for the signature, or NULL for a default.
 * @param certs Optional certificates to include with the request, or NULL.
 * @param flags OCSP_* flags such as OCSP_NOCERTS.
 * @return 1 on success, or 0 on failure.
 */
int OCSP_request_sign(OCSP_REQUEST *req,
    X509 *signer,
    EVP_PKEY *key,
    const EVP_MD *dgst,
    STACK_OF(X509) *certs, unsigned long flags);

/**
 * @brief Return the OCSPResponseStatus enumerated value from an OCSP response.
 * @param resp OCSP response to query.
 * @return Status code such as OCSP_RESPONSE_STATUS_SUCCESSFUL.
 */
int OCSP_response_status(OCSP_RESPONSE *resp);
/**
 * @brief Extract and decode the BasicOCSPResponse from a successful OCSP response.
 * @param resp OCSP response whose responseBytes contain an id-pkix-ocsp-basic payload.
 * @return Newly allocated OCSP_BASICRESP, or NULL on error; free with OCSP_BASICRESP_free().
 */
OCSP_BASICRESP *OCSP_response_get1_basic(OCSP_RESPONSE *resp);

/**
 * @brief Return the signature OCTET STRING from a basic OCSP response (borrowed).
 * @param bs Basic OCSP response to query.
 * @return Internal ASN1_OCTET_STRING pointer to the signature value, or NULL if unset.
 */
const ASN1_OCTET_STRING *OCSP_resp_get0_signature(const OCSP_BASICRESP *bs);
/**
 * @brief Return the signature AlgorithmIdentifier from a basic OCSP response.
 * @param bs Basic OCSP response to query.
 * @return Pointer to the internal signature algorithm, or NULL if unset.
 */
const X509_ALGOR *OCSP_resp_get0_tbs_sigalg(const OCSP_BASICRESP *bs);
/**
 * @brief Return the tbsResponseData from a basic OCSP response.
 * @param bs Basic OCSP response to query.
 * @return Internal OCSP_RESPDATA pointer, or NULL if unset.
 */
const OCSP_RESPDATA *OCSP_resp_get0_respdata(const OCSP_BASICRESP *bs);
/**
 * @brief Locate the signer certificate for a basic OCSP response.
 * @param bs Basic OCSP response whose ResponderID is matched.
 * @param signer Receives an internal pointer to the signer certificate on success.
 * @param extra_certs Optional additional certificates to search, or NULL.
 * @return 1 if a signer was found, or 0 otherwise.
 */
int OCSP_resp_get0_signer(OCSP_BASICRESP *bs, X509 **signer,
    STACK_OF(X509) *extra_certs);

/**
 * @brief Return the number of SingleResponse entries in a basic OCSP response.
 * @param bs Basic OCSP response to query.
 * @return Number of single responses, or 0 if none.
 */
int OCSP_resp_count(OCSP_BASICRESP *bs);
/**
 * @brief Return the SingleResponse at index @p idx in a basic OCSP response.
 * @param bs Basic OCSP response to query.
 * @param idx Zero-based index into the responses sequence.
 * @return Internal OCSP_SINGLERESP pointer, or NULL if @p bs is NULL or @p idx is out of range.
 */
OCSP_SINGLERESP *OCSP_resp_get0(OCSP_BASICRESP *bs, int idx);
/**
 * @brief Return the producedAt time from a basic OCSP response.
 * @param bs Basic OCSP response to query.
 * @return Internal ASN1_GENERALIZEDTIME pointer, or NULL if unset.
 */
const ASN1_GENERALIZEDTIME *OCSP_resp_get0_produced_at(const OCSP_BASICRESP *bs);
/**
 * @brief Return the optional certificate stack embedded in a basic OCSP response.
 * @param bs Basic OCSP response to query.
 * @return Internal STACK_OF(X509), or NULL if none are present.
 */
const STACK_OF(X509) *OCSP_resp_get0_certs(const OCSP_BASICRESP *bs);
/**
 * @brief Return internal pointers to the ResponderID fields of a basic OCSP response.
 * @param bs Basic OCSP response to query.
 * @param pid Receives the byKey OCTET STRING when that form is used; otherwise set to NULL.
 * @param pname Receives the byName X509_NAME when that form is used; otherwise set to NULL.
 * @return 1 on success, or 0 on failure.
 */
int OCSP_resp_get0_id(const OCSP_BASICRESP *bs,
    const ASN1_OCTET_STRING **pid,
    const X509_NAME **pname);
/**
 * @brief Return a copy of the ResponderID from a basic OCSP response.
 * @param bs Basic OCSP response to query.
 * @param pid Receives a new OCTET STRING copy when the ID is byKey; otherwise set to NULL.
 * @param pname Receives a new X509_NAME copy when the ID is byName; otherwise set to NULL.
 * @return 1 on success, or 0 on failure.
 */
int OCSP_resp_get1_id(const OCSP_BASICRESP *bs,
    ASN1_OCTET_STRING **pid,
    X509_NAME **pname);

/**
 * @brief Find the next SingleResponse whose CertID matches @p id.
 * @param bs Basic OCSP response to search.
 * @param id CertID identifying the certificate of interest.
 * @param last Index after which to search, or -1 to start from the beginning.
 * @return Index of the matching SingleResponse, or -1 if not found.
 */
int OCSP_resp_find(OCSP_BASICRESP *bs, OCSP_CERTID *id, int last);
/**
 * @brief Extract certificate status and related times from a SingleResponse.
 * @param single SingleResponse to query.
 * @param reason Optional output for the revocation reason when status is revoked; may be NULL.
 * @param revtime Optional output for the revocation time when status is revoked; may be NULL.
 * @param thisupd Optional output for thisUpdate; may be NULL.
 * @param nextupd Optional output for nextUpdate; may be NULL.
 * @return OCSP_CERTIFICATESTATUS_* value (GOOD, REVOKED, or UNKNOWN).
 */
int OCSP_single_get0_status(OCSP_SINGLERESP *single, int *reason,
    ASN1_GENERALIZEDTIME **revtime,
    ASN1_GENERALIZEDTIME **thisupd,
    ASN1_GENERALIZEDTIME **nextupd);
/**
 * @brief Find a SingleResponse for @p id and return its certificate status fields.
 * @param bs Basic OCSP response to search.
 * @param id CertID identifying the certificate of interest.
 * @param status Receives OCSP_CERTIFICATESTATUS_* (for example GOOD, REVOKED, UNKNOWN).
 * @param reason Optional revocation reason code when status is revoked; may be NULL.
 * @param revtime Optional revocation time when status is revoked; may be NULL.
 * @param thisupd Optional thisUpdate time from the SingleResponse; may be NULL.
 * @param nextupd Optional nextUpdate time from the SingleResponse; may be NULL.
 * @return Index of the matching SingleResponse, or -1 if not found.
 */
int OCSP_resp_find_status(OCSP_BASICRESP *bs, OCSP_CERTID *id, int *status,
    int *reason,
    ASN1_GENERALIZEDTIME **revtime,
    ASN1_GENERALIZEDTIME **thisupd,
    ASN1_GENERALIZEDTIME **nextupd);
/**
 * @brief Check that OCSP thisUpdate/nextUpdate times are acceptable relative to now.
 * @param thisupd thisUpdate time from a SingleResponse (required).
 * @param nextupd Optional nextUpdate time, or NULL if absent.
 * @param sec Allowed skew in seconds comparing thisUpdate to the current time.
 * @param maxsec Maximum age of thisUpdate in seconds, or -1 for no maximum.
 * @return 1 if the times are valid, or 0 otherwise.
 */
int OCSP_check_validity(ASN1_GENERALIZEDTIME *thisupd,
    ASN1_GENERALIZEDTIME *nextupd, long sec, long maxsec);

/**
 * @brief Verify the signature on a signed OCSP request.
 * @param req Signed OCSP request to verify.
 * @param certs Optional additional certificates for path building, or NULL.
 * @param store Trusted certificate store used for verification.
 * @param flags OCSP_* / X509 verify flags controlling verification behaviour.
 * @return 1 on successful verification, or 0 on failure.
 */
int OCSP_request_verify(OCSP_REQUEST *req, STACK_OF(X509) *certs,
    X509_STORE *store, unsigned long flags);

#define OCSP_parse_url(url, host, port, path, ssl) \
    OSSL_HTTP_parse_url(url, ssl, NULL, host, port, NULL, path, NULL, NULL)

/**
 * @brief Compare only the issuer name/key hash fields of two OCSP CertIDs.
 * @param a First CertID.
 * @param b Second CertID.
 * @return 0 if the issuer hashes match, or a nonzero value if they differ.
 */
int OCSP_id_issuer_cmp(const OCSP_CERTID *a, const OCSP_CERTID *b);
/**
 * @brief Compare two OCSP CertID values for equality.
 * @param a First CertID.
 * @param b Second CertID.
 * @return 0 if equal, or a nonzero value if they differ.
 */
int OCSP_id_cmp(const OCSP_CERTID *a, const OCSP_CERTID *b);

/**
 * @brief Return the number of single-request entries in an OCSP request.
 * @param req OCSP request to query.
 * @return Number of OCSP_ONEREQ entries in the requestList.
 */
int OCSP_request_onereq_count(OCSP_REQUEST *req);
/**
 * @brief Return the single-request entry at index @p i in an OCSP request.
 * @param req OCSP request to query.
 * @param i Zero-based index into the requestList.
 * @return Internal OCSP_ONEREQ pointer, or NULL if @p i is out of range.
 */
OCSP_ONEREQ *OCSP_request_onereq_get0(OCSP_REQUEST *req, int i);
/**
 * @brief Return the CertID from a single OCSP request entry.
 * @param one Single-request entry to query.
 * @return Internal OCSP_CERTID pointer (reqCert), or NULL if unset.
 */
OCSP_CERTID *OCSP_onereq_get0_id(OCSP_ONEREQ *one);
/**
 * @brief Return borrowed pointers to the fields of an OCSP CertID.
 * @param piNameHash Optional output for issuerNameHash; may be NULL.
 * @param pmd Optional output for the hash AlgorithmIdentifier OID; may be NULL.
 * @param pikeyHash Optional output for issuerKeyHash; may be NULL.
 * @param pserial Optional output for the certificate serialNumber; may be NULL.
 * @param cid CertID to query.
 * @return 1 on success, or 0 on failure.
 */
int OCSP_id_get0_info(ASN1_OCTET_STRING **piNameHash, ASN1_OBJECT **pmd,
    ASN1_OCTET_STRING **pikeyHash,
    ASN1_INTEGER **pserial, OCSP_CERTID *cid);
/**
 * @brief Test whether an OCSP request carries an optional signature.
 * @param req OCSP request to query.
 * @return 1 if a signature is present, or 0 otherwise.
 */
int OCSP_request_is_signed(OCSP_REQUEST *req);
/**
 * @brief Create an OCSP response with the given status and optional basic response.
 * @param status OCSPResponseStatus value such as OCSP_RESPONSE_STATUS_SUCCESSFUL.
 * @param bs Optional BasicOCSPResponse to embed as responseBytes, or NULL for status-only.
 * @return Newly allocated OCSP_RESPONSE, or NULL on error; free with OCSP_RESPONSE_free().
 */
OCSP_RESPONSE *OCSP_response_create(int status, OCSP_BASICRESP *bs);
/**
 * @brief Append a SingleResponse with certificate status to a basic OCSP response.
 * @param rsp Basic response being constructed.
 * @param cid CertID identifying the certificate (ownership transferred on success).
 * @param status OCSP_CERTIFICATESTATUS_* value for the response.
 * @param reason Revocation reason when @p status is revoked; otherwise ignored.
 * @param revtime Revocation time when @p status is revoked; otherwise may be NULL.
 * @param thisupd thisUpdate time for the SingleResponse.
 * @param nextupd Optional nextUpdate time, or NULL.
 * @return Internal OCSP_SINGLERESP pointer on success, or NULL on error.
 */
OCSP_SINGLERESP *OCSP_basic_add1_status(OCSP_BASICRESP *rsp,
    OCSP_CERTID *cid,
    int status, int reason,
    ASN1_TIME *revtime,
    ASN1_TIME *thisupd,
    ASN1_TIME *nextupd);
/**
 * @brief Add a certificate to the optional certs field of a basic OCSP response.
 * @param resp Basic response to update.
 * @param cert Certificate to include (reference count increased).
 * @return 1 on success, or 0 on failure.
 */
int OCSP_basic_add1_cert(OCSP_BASICRESP *resp, X509 *cert);
/**
 * @brief Sign a basic OCSP response with @p signer and @p key.
 * @param brsp Basic response whose tbsResponseData is signed.
 * @param signer Certificate identifying the responder.
 * @param key Private key corresponding to @p signer.
 * @param dgst Digest algorithm for the signature, or NULL for a default.
 * @param certs Optional certificates to include with the response, or NULL.
 * @param flags OCSP_* flags such as OCSP_NOCERTS or OCSP_RESPID_KEY.
 * @return 1 on success, or 0 on failure.
 */
int OCSP_basic_sign(OCSP_BASICRESP *brsp,
    X509 *signer, EVP_PKEY *key, const EVP_MD *dgst,
    STACK_OF(X509) *certs, unsigned long flags);
/**
 * @brief Sign a basic OCSP response using an initialized digest/sign context.
 * @param brsp Basic response whose tbsResponseData is signed.
 * @param signer Certificate identifying the responder.
 * @param ctx Digest context already set up for signing with the responder key.
 * @param certs Optional certificates to include with the response, or NULL.
 * @param flags OCSP_* flags such as OCSP_NOCERTS or OCSP_RESPID_KEY.
 * @return 1 on success, or 0 on failure.
 */
int OCSP_basic_sign_ctx(OCSP_BASICRESP *brsp,
    X509 *signer, EVP_MD_CTX *ctx,
    STACK_OF(X509) *certs, unsigned long flags);
/**
 * @brief Set a ResponderID to the subject name of @p cert.
 * @param respid ResponderID to update.
 * @param cert Certificate whose subject name becomes the byName identity.
 * @return 1 on success, or 0 on failure.
 */
int OCSP_RESPID_set_by_name(OCSP_RESPID *respid, X509 *cert);
/**
 * @brief Set a ResponderID to the SHA-1 hash of @p cert's public key, with provider selection.
 * @param respid ResponderID to update.
 * @param cert Certificate whose public key is hashed for the byKey identity.
 * @param libctx Library context for algorithm fetches, or NULL for the default.
 * @param propq Property query for provider selection, or NULL.
 * @return 1 on success, or 0 on failure.
 */
int OCSP_RESPID_set_by_key_ex(OCSP_RESPID *respid, X509 *cert,
    OSSL_LIB_CTX *libctx, const char *propq);
/**
 * @brief Set a ResponderID to the SHA-1 hash of @p cert's public key (default libctx).
 * @param respid ResponderID to update.
 * @param cert Certificate whose public key is hashed for the byKey identity.
 * @return 1 on success, or 0 on failure.
 */
int OCSP_RESPID_set_by_key(OCSP_RESPID *respid, X509 *cert);
/**
 * @brief Test whether a ResponderID matches @p cert by name or key hash.
 * @param respid ResponderID to compare.
 * @param cert Candidate responder certificate.
 * @param libctx Library context for key-hash digests, or NULL for the default.
 * @param propq Property query for provider selection, or NULL.
 * @return 1 if @p respid matches @p cert, or 0 otherwise.
 */
int OCSP_RESPID_match_ex(OCSP_RESPID *respid, X509 *cert, OSSL_LIB_CTX *libctx,
    const char *propq);
/**
 * @brief Test whether a ResponderID matches @p cert by name or key hash (default libctx).
 * @param respid ResponderID to compare.
 * @param cert Candidate responder certificate.
 * @return 1 if @p respid matches @p cert, or 0 otherwise.
 */
int OCSP_RESPID_match(OCSP_RESPID *respid, X509 *cert);

/**
 * @brief Create a CrlID OCSP extension from optional URL, CRL number, and time.
 * @param url Optional CRL distribution URL string, or NULL.
 * @param n Optional CRL number; when non-NULL its value is encoded into the extension.
 * @param tim Optional GeneralizedTime string (YYYYMMDDHHMMSSZ) for crlTime, or NULL.
 * @return New X509_EXTENSION, or NULL on error; free with X509_EXTENSION_free().
 */
X509_EXTENSION *OCSP_crlID_new(const char *url, long *n, char *tim);

/**
 * @brief Create an AcceptableResponses OCSP extension from OID strings.
 * @param oids NULL-terminated array of textual OIDs naming acceptable response types.
 * @return New X509_EXTENSION, or NULL on error; free with X509_EXTENSION_free().
 */
X509_EXTENSION *OCSP_accept_responses_new(char **oids);

/**
 * @brief Create an ArchiveCutoff OCSP extension from a time string.
 * @param tim GeneralizedTime string (YYYYMMDDHHMMSSZ) for the archive cutoff.
 * @return New X509_EXTENSION, or NULL on error; free with X509_EXTENSION_free().
 */
X509_EXTENSION *OCSP_archive_cutoff_new(char *tim);

/**
 * @brief Create a ServiceLocator OCSP extension for @p issuer and locator URLs.
 * @param issuer Issuer name placed in the ServiceLocator.
 * @param urls NULL-terminated array of OCSP service URI strings.
 * @return New X509_EXTENSION, or NULL on error; free with X509_EXTENSION_free().
 */
X509_EXTENSION *OCSP_url_svcloc_new(const X509_NAME *issuer, const char **urls);

/**
 * @brief Return the number of extensions on an OCSP request.
 * @param x OCSP request to query.
 * @return Extension count, or 0 if none.
 */
int OCSP_REQUEST_get_ext_count(OCSP_REQUEST *x);
/**
 * @brief Find the next OCSP request extension with NID @p nid.
 * @param x OCSP request whose extensions are searched.
 * @param nid NID of the extension type to find.
 * @param lastpos Index after which to search, or -1 to start from the beginning.
 * @return Extension index, or -1 if not found.
 */
int OCSP_REQUEST_get_ext_by_NID(OCSP_REQUEST *x, int nid, int lastpos);
/**
 * @brief Find the next OCSP request extension with object identifier @p obj.
 * @param x OCSP request whose extensions are searched.
 * @param obj ASN.1 object identifier to match.
 * @param lastpos Index after which to search, or -1 to start from the beginning.
 * @return Extension index, or -1 if not found.
 */
int OCSP_REQUEST_get_ext_by_OBJ(OCSP_REQUEST *x, const ASN1_OBJECT *obj,
    int lastpos);
/**
 * @brief Find the next OCSP request extension with criticality @p crit.
 * @param x OCSP request whose extensions are searched.
 * @param crit Nonzero to match critical extensions, zero for non-critical.
 * @param lastpos Index after which to search, or -1 to start from the beginning.
 * @return Extension index, or -1 if not found.
 */
int OCSP_REQUEST_get_ext_by_critical(OCSP_REQUEST *x, int crit, int lastpos);
/**
 * @brief Return the OCSP request extension at index @p loc.
 * @param x OCSP request to query.
 * @param loc Zero-based extension index.
 * @return Internal X509_EXTENSION pointer, or NULL if @p loc is out of range.
 */
X509_EXTENSION *OCSP_REQUEST_get_ext(OCSP_REQUEST *x, int loc);
/**
 * @brief Remove and return the OCSP request extension at index @p loc.
 * @param x OCSP request to update.
 * @param loc Zero-based extension index.
 * @return Detached X509_EXTENSION the caller must free, or NULL if @p loc is invalid.
 */
X509_EXTENSION *OCSP_REQUEST_delete_ext(OCSP_REQUEST *x, int loc);
/**
 * @brief Decode the first OCSP request extension of type @p nid into its native structure.
 * @param x OCSP request to query.
 * @param nid NID of the extension type to extract.
 * @param crit Optional output set to 1 if critical, 0 if not, or -1 on error; may be NULL.
 * @param idx Optional in/out extension index for X509V3_get_d2i-style iteration; may be NULL.
 * @return Newly allocated decoded extension value, or NULL if absent or on error.
 */
void *OCSP_REQUEST_get1_ext_d2i(OCSP_REQUEST *x, int nid, int *crit,
    int *idx);
/**
 * @brief Encode @p value as an OCSP request extension of type @p nid and append it.
 * @param x OCSP request to update.
 * @param nid NID of the extension type to add.
 * @param value Native extension structure encoded via i2d.
 * @param crit Nonzero to mark the extension critical.
 * @param flags X509V3_ADD_* flags controlling replacement behaviour.
 * @return 1 on success, 0 on error, or -1 on a fatal error.
 */
int OCSP_REQUEST_add1_ext_i2d(OCSP_REQUEST *x, int nid, void *value, int crit,
    unsigned long flags);
/**
 * @brief Insert extension @p ex into an OCSP request at position @p loc.
 * @param x OCSP request to update.
 * @param ex Extension to add (duplicated into the request).
 * @param loc Insertion index, or -1 to append.
 * @return 1 on success, or 0 on failure.
 */
int OCSP_REQUEST_add_ext(OCSP_REQUEST *x, X509_EXTENSION *ex, int loc);

/**
 * @brief Return the number of extensions on a single OCSP request entry.
 * @param x Single OCSP request entry to query.
 * @return Extension count, or 0 if none.
 */
int OCSP_ONEREQ_get_ext_count(OCSP_ONEREQ *x);
/**
 * @brief Find the next single-request extension with NID @p nid.
 * @param x Single OCSP request entry whose extensions are searched.
 * @param nid NID of the extension type to find.
 * @param lastpos Index after which to search, or -1 to start from the beginning.
 * @return Extension index, or -1 if not found.
 */
int OCSP_ONEREQ_get_ext_by_NID(OCSP_ONEREQ *x, int nid, int lastpos);
/**
 * @brief Find the next single-request extension with object identifier @p obj.
 * @param x Single OCSP request entry whose extensions are searched.
 * @param obj ASN.1 object identifier to match.
 * @param lastpos Index after which to search, or -1 to start from the beginning.
 * @return Extension index, or -1 if not found.
 */
int OCSP_ONEREQ_get_ext_by_OBJ(OCSP_ONEREQ *x, const ASN1_OBJECT *obj, int lastpos);
/**
 * @brief Find the next single-request extension with criticality @p crit.
 * @param x Single OCSP request entry whose extensions are searched.
 * @param crit Nonzero to match critical extensions, zero for non-critical.
 * @param lastpos Index after which to search, or -1 to start from the beginning.
 * @return Extension index, or -1 if not found.
 */
int OCSP_ONEREQ_get_ext_by_critical(OCSP_ONEREQ *x, int crit, int lastpos);
/**
 * @brief Return the single-request extension at index @p loc.
 * @param x Single OCSP request entry to query.
 * @param loc Zero-based extension index.
 * @return Internal X509_EXTENSION pointer, or NULL if @p loc is out of range.
 */
X509_EXTENSION *OCSP_ONEREQ_get_ext(OCSP_ONEREQ *x, int loc);
/**
 * @brief Remove and return the single-request extension at index @p loc.
 * @param x Single OCSP request entry to update.
 * @param loc Zero-based extension index.
 * @return Detached X509_EXTENSION the caller must free, or NULL if @p loc is invalid.
 */
X509_EXTENSION *OCSP_ONEREQ_delete_ext(OCSP_ONEREQ *x, int loc);
/**
 * @brief Decode the first single-request extension of type @p nid into its native structure.
 * @param x Single OCSP request entry to query.
 * @param nid NID of the extension type to extract.
 * @param crit Optional output set to 1 if critical, 0 if not, or -1 on error; may be NULL.
 * @param idx Optional in/out extension index for X509V3_get_d2i-style iteration; may be NULL.
 * @return Newly allocated decoded extension value, or NULL if absent or on error.
 */
void *OCSP_ONEREQ_get1_ext_d2i(OCSP_ONEREQ *x, int nid, int *crit, int *idx);
/**
 * @brief Encode @p value as a single-request extension of type @p nid and append it.
 * @param x Single OCSP request entry to update.
 * @param nid NID of the extension type to add.
 * @param value Native extension structure encoded via i2d.
 * @param crit Nonzero to mark the extension critical.
 * @param flags X509V3_ADD_* flags controlling replacement behaviour.
 * @return 1 on success, 0 on error, or -1 on a fatal error.
 */
int OCSP_ONEREQ_add1_ext_i2d(OCSP_ONEREQ *x, int nid, void *value, int crit,
    unsigned long flags);
/**
 * @brief Insert extension @p ex into a single OCSP request at position @p loc.
 * @param x Single OCSP request entry to update.
 * @param ex Extension to add (duplicated into the request).
 * @param loc Insertion index, or -1 to append.
 * @return 1 on success, or 0 on failure.
 */
int OCSP_ONEREQ_add_ext(OCSP_ONEREQ *x, X509_EXTENSION *ex, int loc);

/**
 * @brief Return the number of extensions on a basic OCSP response.
 * @param x Basic OCSP response to query.
 * @return Extension count, or 0 if none.
 */
int OCSP_BASICRESP_get_ext_count(OCSP_BASICRESP *x);
/**
 * @brief Find the next basic-response extension with NID @p nid.
 * @param x Basic OCSP response whose extensions are searched.
 * @param nid NID of the extension type to find.
 * @param lastpos Index after which to search, or -1 to start from the beginning.
 * @return Extension index, or -1 if not found.
 */
int OCSP_BASICRESP_get_ext_by_NID(OCSP_BASICRESP *x, int nid, int lastpos);
/**
 * @brief Find the next basic-response extension with object identifier @p obj.
 * @param x Basic OCSP response whose extensions are searched.
 * @param obj ASN.1 object identifier to match.
 * @param lastpos Index after which to search, or -1 to start from the beginning.
 * @return Extension index, or -1 if not found.
 */
int OCSP_BASICRESP_get_ext_by_OBJ(OCSP_BASICRESP *x, const ASN1_OBJECT *obj,
    int lastpos);
/**
 * @brief Find the next basic-response extension with criticality @p crit.
 * @param x Basic OCSP response whose extensions are searched.
 * @param crit Nonzero to match critical extensions, zero for non-critical.
 * @param lastpos Index after which to search, or -1 to start from the beginning.
 * @return Extension index, or -1 if not found.
 */
int OCSP_BASICRESP_get_ext_by_critical(OCSP_BASICRESP *x, int crit,
    int lastpos);
/**
 * @brief Return the basic-response extension at index @p loc.
 * @param x Basic OCSP response to query.
 * @param loc Zero-based extension index.
 * @return Internal X509_EXTENSION pointer, or NULL if @p loc is out of range.
 */
X509_EXTENSION *OCSP_BASICRESP_get_ext(OCSP_BASICRESP *x, int loc);
/**
 * @brief Remove and return the basic-response extension at index @p loc.
 * @param x Basic OCSP response to update.
 * @param loc Zero-based extension index.
 * @return Detached X509_EXTENSION the caller must free, or NULL if @p loc is invalid.
 */
X509_EXTENSION *OCSP_BASICRESP_delete_ext(OCSP_BASICRESP *x, int loc);
/**
 * @brief Decode the first basic-response extension of type @p nid into its native structure.
 * @param x Basic OCSP response to query.
 * @param nid NID of the extension type to extract.
 * @param crit Optional output set to 1 if critical, 0 if not, or -1 on error; may be NULL.
 * @param idx Optional in/out extension index for X509V3_get_d2i-style iteration; may be NULL.
 * @return Newly allocated decoded extension value, or NULL if absent or on error.
 */
void *OCSP_BASICRESP_get1_ext_d2i(OCSP_BASICRESP *x, int nid, int *crit,
    int *idx);
/**
 * @brief Encode @p value as a basic-response extension of type @p nid and append it.
 * @param x Basic OCSP response to update.
 * @param nid NID of the extension type to add.
 * @param value Native extension structure encoded via i2d.
 * @param crit Nonzero to mark the extension critical.
 * @param flags X509V3_ADD_* flags controlling replacement behaviour.
 * @return 1 on success, 0 on error, or -1 on a fatal error.
 */
int OCSP_BASICRESP_add1_ext_i2d(OCSP_BASICRESP *x, int nid, void *value,
    int crit, unsigned long flags);
/**
 * @brief Insert extension @p ex into a basic OCSP response at position @p loc.
 * @param x Basic OCSP response to update.
 * @param ex Extension to add (duplicated into the response).
 * @param loc Insertion index, or -1 to append.
 * @return 1 on success, or 0 on failure.
 */
int OCSP_BASICRESP_add_ext(OCSP_BASICRESP *x, X509_EXTENSION *ex, int loc);

/**
 * @brief Return the number of extensions on an OCSP SingleResponse.
 * @param x SingleResponse to query.
 * @return Extension count, or 0 if none.
 */
int OCSP_SINGLERESP_get_ext_count(OCSP_SINGLERESP *x);
/**
 * @brief Find the next SingleResponse extension with NID @p nid.
 * @param x SingleResponse whose extensions are searched.
 * @param nid NID of the extension type to find.
 * @param lastpos Index after which to search, or -1 to start from the beginning.
 * @return Extension index, or -1 if not found.
 */
int OCSP_SINGLERESP_get_ext_by_NID(OCSP_SINGLERESP *x, int nid, int lastpos);
/**
 * @brief Find the next SingleResponse extension with object identifier @p obj.
 * @param x SingleResponse whose extensions are searched.
 * @param obj ASN.1 object identifier to match.
 * @param lastpos Index after which to search, or -1 to start from the beginning.
 * @return Extension index, or -1 if not found.
 */
int OCSP_SINGLERESP_get_ext_by_OBJ(OCSP_SINGLERESP *x, const ASN1_OBJECT *obj,
    int lastpos);
/**
 * @brief Find the next SingleResponse extension with criticality @p crit.
 * @param x SingleResponse whose extensions are searched.
 * @param crit Nonzero to match critical extensions, zero for non-critical.
 * @param lastpos Index after which to search, or -1 to start from the beginning.
 * @return Extension index, or -1 if not found.
 */
int OCSP_SINGLERESP_get_ext_by_critical(OCSP_SINGLERESP *x, int crit,
    int lastpos);
/**
 * @brief Return the SingleResponse extension at index @p loc.
 * @param x SingleResponse to query.
 * @param loc Zero-based extension index.
 * @return Internal X509_EXTENSION pointer, or NULL if @p loc is out of range.
 */
X509_EXTENSION *OCSP_SINGLERESP_get_ext(OCSP_SINGLERESP *x, int loc);
/**
 * @brief Remove and return the SingleResponse extension at index @p loc.
 * @param x SingleResponse to update.
 * @param loc Zero-based extension index.
 * @return Detached X509_EXTENSION the caller must free, or NULL if @p loc is invalid.
 */
X509_EXTENSION *OCSP_SINGLERESP_delete_ext(OCSP_SINGLERESP *x, int loc);
/**
 * @brief Decode the first SingleResponse extension of type @p nid into its native structure.
 * @param x SingleResponse to query.
 * @param nid NID of the extension type to extract.
 * @param crit Optional output set to 1 if critical, 0 if not, or -1 on error; may be NULL.
 * @param idx Optional in/out extension index for X509V3_get_d2i-style iteration; may be NULL.
 * @return Newly allocated decoded extension value, or NULL if absent or on error.
 */
void *OCSP_SINGLERESP_get1_ext_d2i(OCSP_SINGLERESP *x, int nid, int *crit,
    int *idx);
/**
 * @brief Encode @p value as a SingleResponse extension of type @p nid and append it.
 * @param x SingleResponse to update.
 * @param nid NID of the extension type to add.
 * @param value Native extension structure encoded via i2d.
 * @param crit Nonzero to mark the extension critical.
 * @param flags X509V3_ADD_* flags controlling replacement behaviour.
 * @return 1 on success, 0 on error, or -1 on a fatal error.
 */
int OCSP_SINGLERESP_add1_ext_i2d(OCSP_SINGLERESP *x, int nid, void *value,
    int crit, unsigned long flags);
/**
 * @brief Insert extension @p ex into a SingleResponse at position @p loc.
 * @param x SingleResponse to update.
 * @param ex Extension to add (duplicated into the SingleResponse).
 * @param loc Insertion index, or -1 to append.
 * @return 1 on success, or 0 on failure.
 */
int OCSP_SINGLERESP_add_ext(OCSP_SINGLERESP *x, X509_EXTENSION *ex, int loc);
/**
 * @brief Return the CertID from a SingleResponse.
 * @param x SingleResponse to query.
 * @return Internal OCSP_CERTID pointer, or NULL if unset.
 */
const OCSP_CERTID *OCSP_SINGLERESP_get0_id(const OCSP_SINGLERESP *x);

/**
 * @brief Allocate an empty OCSP SingleResponse.
 * @return New OCSP_SINGLERESP, or NULL on allocation failure.
 */
OCSP_SINGLERESP *OCSP_SINGLERESP_new(void);
/**
 * @brief Free an OCSP SingleResponse and its contents.
 * @param a Value to free, or NULL.
 */
void OCSP_SINGLERESP_free(OCSP_SINGLERESP *a);
/**
 * @brief Decode an OCSP SingleResponse from DER.
 * @param a Optional destination pointer updated to the result, or NULL.
 * @param in Address of a pointer to the DER input; advanced past the decoded value.
 * @param len Number of bytes available at *@p in.
 * @return Decoded OCSP_SINGLERESP, or NULL on error.
 */
OCSP_SINGLERESP *d2i_OCSP_SINGLERESP(OCSP_SINGLERESP **a, const unsigned char **in, long len);
/**
 * @brief Encode an OCSP SingleResponse to DER.
 * @param a Value to encode.
 * @param out Destination pointer updated like a standard i2d encoder, or NULL to measure length.
 * @return Number of bytes encoded, or a negative value on error.
 */
int i2d_OCSP_SINGLERESP(const OCSP_SINGLERESP *a, unsigned char **out);
/**
 * @brief Return the ASN.1 item descriptor for OCSP_SINGLERESP.
 * @return Pointer to the static ASN1_ITEM for OCSP_SINGLERESP.
 */
const ASN1_ITEM *OCSP_SINGLERESP_it(void);
/**
 * @brief Allocate an empty OCSP CertStatus.
 * @return New OCSP_CERTSTATUS, or NULL on allocation failure.
 */
OCSP_CERTSTATUS *OCSP_CERTSTATUS_new(void);
/**
 * @brief Free a OCSP CertStatus and its contents.
 * @param a Value to free, or NULL.
 */
void OCSP_CERTSTATUS_free(OCSP_CERTSTATUS *a);
/**
 * @brief Decode a OCSP CertStatus from DER.
 * @param a Optional destination pointer updated to the result, or NULL.
 * @param in Address of a pointer to the DER input; advanced past the decoded value.
 * @param len Number of bytes available at *@p in.
 * @return Decoded OCSP_CERTSTATUS, or NULL on error.
 */
OCSP_CERTSTATUS *d2i_OCSP_CERTSTATUS(OCSP_CERTSTATUS **a, const unsigned char **in, long len);
/**
 * @brief Encode a OCSP CertStatus to DER.
 * @param a Value to encode.
 * @param out Destination pointer updated like a standard i2d encoder, or NULL to measure length.
 * @return Number of bytes encoded, or a negative value on error.
 */
int i2d_OCSP_CERTSTATUS(const OCSP_CERTSTATUS *a, unsigned char **out);
/**
 * @brief Return the ASN.1 item descriptor for OCSP_CERTSTATUS.
 * @return Pointer to the static ASN1_ITEM for OCSP_CERTSTATUS.
 */
const ASN1_ITEM *OCSP_CERTSTATUS_it(void);
/**
 * @brief Allocate an empty OCSP RevokedInfo.
 * @return New OCSP_REVOKEDINFO, or NULL on allocation failure.
 */
OCSP_REVOKEDINFO *OCSP_REVOKEDINFO_new(void);
/**
 * @brief Free an OCSP RevokedInfo and its contents.
 * @param a Value to free, or NULL.
 */
void OCSP_REVOKEDINFO_free(OCSP_REVOKEDINFO *a);
/**
 * @brief Decode an OCSP RevokedInfo from DER.
 * @param a Optional destination pointer updated to the result, or NULL.
 * @param in Address of a pointer to the DER input; advanced past the decoded value.
 * @param len Number of bytes available at *@p in.
 * @return Decoded OCSP_REVOKEDINFO, or NULL on error.
 */
OCSP_REVOKEDINFO *d2i_OCSP_REVOKEDINFO(OCSP_REVOKEDINFO **a, const unsigned char **in, long len);
/**
 * @brief Encode an OCSP RevokedInfo to DER.
 * @param a Value to encode.
 * @param out Destination pointer updated like a standard i2d encoder, or NULL to measure length.
 * @return Number of bytes encoded, or a negative value on error.
 */
int i2d_OCSP_REVOKEDINFO(const OCSP_REVOKEDINFO *a, unsigned char **out);
/**
 * @brief Return the ASN.1 item descriptor for OCSP_REVOKEDINFO.
 * @return Pointer to the static ASN1_ITEM for OCSP_REVOKEDINFO.
 */
const ASN1_ITEM *OCSP_REVOKEDINFO_it(void);
/**
 * @brief Allocate an empty OCSP BasicOCSPResponse.
 * @return New OCSP_BASICRESP, or NULL on allocation failure.
 */
OCSP_BASICRESP *OCSP_BASICRESP_new(void);
/**
 * @brief Free an OCSP BasicOCSPResponse and its contents.
 * @param a Value to free, or NULL.
 */
void OCSP_BASICRESP_free(OCSP_BASICRESP *a);
/**
 * @brief Decode an OCSP BasicOCSPResponse from DER.
 * @param a Optional destination pointer updated to the result, or NULL.
 * @param in Address of a pointer to the DER input; advanced past the decoded value.
 * @param len Number of bytes available at *@p in.
 * @return Decoded OCSP_BASICRESP, or NULL on error.
 */
OCSP_BASICRESP *d2i_OCSP_BASICRESP(OCSP_BASICRESP **a, const unsigned char **in, long len);
/**
 * @brief Encode an OCSP BasicOCSPResponse to DER.
 * @param a Value to encode.
 * @param out Destination pointer updated like a standard i2d encoder, or NULL to measure length.
 * @return Number of bytes encoded, or a negative value on error.
 */
int i2d_OCSP_BASICRESP(const OCSP_BASICRESP *a, unsigned char **out);
/**
 * @brief Return the ASN.1 item descriptor for OCSP_BASICRESP.
 * @return Pointer to the static ASN1_ITEM for OCSP_BASICRESP.
 */
const ASN1_ITEM *OCSP_BASICRESP_it(void);
/**
 * @brief Allocate an empty OCSP ResponseData.
 * @return New OCSP_RESPDATA, or NULL on allocation failure.
 */
OCSP_RESPDATA *OCSP_RESPDATA_new(void);
/**
 * @brief Free a OCSP ResponseData and its contents.
 * @param a Value to free, or NULL.
 */
void OCSP_RESPDATA_free(OCSP_RESPDATA *a);
/**
 * @brief Decode a OCSP ResponseData from DER.
 * @param a Optional destination pointer updated to the result, or NULL.
 * @param in Address of a pointer to the DER input; advanced past the decoded value.
 * @param len Number of bytes available at *@p in.
 * @return Decoded OCSP_RESPDATA, or NULL on error.
 */
OCSP_RESPDATA *d2i_OCSP_RESPDATA(OCSP_RESPDATA **a, const unsigned char **in, long len);
/**
 * @brief Encode a OCSP ResponseData to DER.
 * @param a Value to encode.
 * @param out Destination pointer updated like a standard i2d encoder, or NULL to measure length.
 * @return Number of bytes encoded, or a negative value on error.
 */
int i2d_OCSP_RESPDATA(const OCSP_RESPDATA *a, unsigned char **out);
/**
 * @brief Return the ASN.1 item descriptor for OCSP_RESPDATA.
 * @return Pointer to the static ASN1_ITEM for OCSP_RESPDATA.
 */
const ASN1_ITEM *OCSP_RESPDATA_it(void);
/**
 * @brief Allocate an empty OCSP ResponderID.
 * @return New OCSP_RESPID, or NULL on allocation failure.
 */
OCSP_RESPID *OCSP_RESPID_new(void);
/**
 * @brief Free a OCSP ResponderID and its contents.
 * @param a Value to free, or NULL.
 */
void OCSP_RESPID_free(OCSP_RESPID *a);
/**
 * @brief Decode a OCSP ResponderID from DER.
 * @param a Optional destination pointer updated to the result, or NULL.
 * @param in Address of a pointer to the DER input; advanced past the decoded value.
 * @param len Number of bytes available at *@p in.
 * @return Decoded OCSP_RESPID, or NULL on error.
 */
OCSP_RESPID *d2i_OCSP_RESPID(OCSP_RESPID **a, const unsigned char **in, long len);
/**
 * @brief Encode a OCSP ResponderID to DER.
 * @param a Value to encode.
 * @param out Destination pointer updated like a standard i2d encoder, or NULL to measure length.
 * @return Number of bytes encoded, or a negative value on error.
 */
int i2d_OCSP_RESPID(const OCSP_RESPID *a, unsigned char **out);
/**
 * @brief Return the ASN.1 item descriptor for OCSP_RESPID.
 * @return Pointer to the static ASN1_ITEM for OCSP_RESPID.
 */
const ASN1_ITEM *OCSP_RESPID_it(void);
/**
 * @brief Allocate an empty OCSP Response.
 * @return New OCSP_RESPONSE, or NULL on allocation failure.
 */
OCSP_RESPONSE *OCSP_RESPONSE_new(void);
/**
 * @brief Free a OCSP Response and its contents.
 * @param a Value to free, or NULL.
 */
void OCSP_RESPONSE_free(OCSP_RESPONSE *a);
/**
 * @brief Decode a OCSP Response from DER.
 * @param a Optional destination pointer updated to the result, or NULL.
 * @param in Address of a pointer to the DER input; advanced past the decoded value.
 * @param len Number of bytes available at *@p in.
 * @return Decoded OCSP_RESPONSE, or NULL on error.
 */
OCSP_RESPONSE *d2i_OCSP_RESPONSE(OCSP_RESPONSE **a, const unsigned char **in, long len);
/**
 * @brief Encode a OCSP Response to DER.
 * @param a Value to encode.
 * @param out Destination pointer updated like a standard i2d encoder, or NULL to measure length.
 * @return Number of bytes encoded, or a negative value on error.
 */
int i2d_OCSP_RESPONSE(const OCSP_RESPONSE *a, unsigned char **out);
/**
 * @brief Return the ASN.1 item descriptor for OCSP_RESPONSE.
 * @return Pointer to the static ASN1_ITEM for OCSP_RESPONSE.
 */
const ASN1_ITEM *OCSP_RESPONSE_it(void);
/**
 * @brief Allocate an empty OCSP ResponseBytes.
 * @return New OCSP_RESPBYTES, or NULL on allocation failure.
 */
OCSP_RESPBYTES *OCSP_RESPBYTES_new(void);
/**
 * @brief Free an OCSP ResponseBytes and its contents.
 * @param a Value to free, or NULL.
 */
void OCSP_RESPBYTES_free(OCSP_RESPBYTES *a);
/**
 * @brief Decode an OCSP ResponseBytes from DER.
 * @param a Optional destination pointer updated to the result, or NULL.
 * @param in Address of a pointer to the DER input; advanced past the decoded value.
 * @param len Number of bytes available at *@p in.
 * @return Decoded OCSP_RESPBYTES, or NULL on error.
 */
OCSP_RESPBYTES *d2i_OCSP_RESPBYTES(OCSP_RESPBYTES **a, const unsigned char **in, long len);
/**
 * @brief Encode an OCSP ResponseBytes to DER.
 * @param a Value to encode.
 * @param out Destination pointer updated like a standard i2d encoder, or NULL to measure length.
 * @return Number of bytes encoded, or a negative value on error.
 */
int i2d_OCSP_RESPBYTES(const OCSP_RESPBYTES *a, unsigned char **out);
/**
 * @brief Return the ASN.1 item descriptor for OCSP_RESPBYTES.
 * @return Pointer to the static ASN1_ITEM for OCSP_RESPBYTES.
 */
const ASN1_ITEM *OCSP_RESPBYTES_it(void);
/**
 * @brief Allocate an empty OCSP single Request entry.
 * @return New OCSP_ONEREQ, or NULL on allocation failure.
 */
OCSP_ONEREQ *OCSP_ONEREQ_new(void);
/**
 * @brief Free an OCSP single Request entry and its contents.
 * @param a Value to free, or NULL.
 */
void OCSP_ONEREQ_free(OCSP_ONEREQ *a);
/**
 * @brief Decode an OCSP single Request entry from DER.
 * @param a Optional destination pointer updated to the result, or NULL.
 * @param in Address of a pointer to the DER input; advanced past the decoded value.
 * @param len Number of bytes available at *@p in.
 * @return Decoded OCSP_ONEREQ, or NULL on error.
 */
OCSP_ONEREQ *d2i_OCSP_ONEREQ(OCSP_ONEREQ **a, const unsigned char **in, long len);
/**
 * @brief Encode an OCSP single Request entry to DER.
 * @param a Value to encode.
 * @param out Destination pointer updated like a standard i2d encoder, or NULL to measure length.
 * @return Number of bytes encoded, or a negative value on error.
 */
int i2d_OCSP_ONEREQ(const OCSP_ONEREQ *a, unsigned char **out);
/**
 * @brief Return the ASN.1 item descriptor for OCSP_ONEREQ.
 * @return Pointer to the static ASN1_ITEM for OCSP_ONEREQ.
 */
const ASN1_ITEM *OCSP_ONEREQ_it(void);
/**
 * @brief Allocate an empty OCSP CertID.
 * @return New OCSP_CERTID, or NULL on allocation failure.
 */
OCSP_CERTID *OCSP_CERTID_new(void);
/**
 * @brief Free an OCSP CertID and its contents.
 * @param a Value to free, or NULL.
 */
void OCSP_CERTID_free(OCSP_CERTID *a);
/**
 * @brief Decode an OCSP CertID from DER.
 * @param a Optional destination pointer updated to the result, or NULL.
 * @param in Address of a pointer to the DER input; advanced past the decoded value.
 * @param len Number of bytes available at *@p in.
 * @return Decoded OCSP_CERTID, or NULL on error.
 */
OCSP_CERTID *d2i_OCSP_CERTID(OCSP_CERTID **a, const unsigned char **in, long len);
/**
 * @brief Encode an OCSP CertID to DER.
 * @param a Value to encode.
 * @param out Destination pointer updated like a standard i2d encoder, or NULL to measure length.
 * @return Number of bytes encoded, or a negative value on error.
 */
int i2d_OCSP_CERTID(const OCSP_CERTID *a, unsigned char **out);
/**
 * @brief Return the ASN.1 item descriptor for OCSP_CERTID.
 * @return Pointer to the static ASN1_ITEM for OCSP_CERTID.
 */
const ASN1_ITEM *OCSP_CERTID_it(void);
/**
 * @brief Allocate an empty OCSP Request.
 * @return New OCSP_REQUEST, or NULL on allocation failure.
 */
OCSP_REQUEST *OCSP_REQUEST_new(void);
/**
 * @brief Free an OCSP Request and its contents.
 * @param a Value to free, or NULL.
 */
void OCSP_REQUEST_free(OCSP_REQUEST *a);
/**
 * @brief Decode an OCSP Request from DER.
 * @param a Optional destination pointer updated to the result, or NULL.
 * @param in Address of a pointer to the DER input; advanced past the decoded value.
 * @param len Number of bytes available at *@p in.
 * @return Decoded OCSP_REQUEST, or NULL on error.
 */
OCSP_REQUEST *d2i_OCSP_REQUEST(OCSP_REQUEST **a, const unsigned char **in, long len);
/**
 * @brief Encode an OCSP Request to DER.
 * @param a Value to encode.
 * @param out Destination pointer updated like a standard i2d encoder, or NULL to measure length.
 * @return Number of bytes encoded, or a negative value on error.
 */
int i2d_OCSP_REQUEST(const OCSP_REQUEST *a, unsigned char **out);
/**
 * @brief Return the ASN.1 item descriptor for OCSP_REQUEST.
 * @return Pointer to the static ASN1_ITEM for OCSP_REQUEST.
 */
const ASN1_ITEM *OCSP_REQUEST_it(void);
/**
 * @brief Allocate an empty OCSP Signature.
 * @return New OCSP_SIGNATURE, or NULL on allocation failure.
 */
OCSP_SIGNATURE *OCSP_SIGNATURE_new(void);
/**
 * @brief Free an OCSP Signature and its contents.
 * @param a Value to free, or NULL.
 */
void OCSP_SIGNATURE_free(OCSP_SIGNATURE *a);
/**
 * @brief Decode an OCSP Signature from DER.
 * @param a Optional destination pointer updated to the result, or NULL.
 * @param in Address of a pointer to the DER input; advanced past the decoded value.
 * @param len Number of bytes available at *@p in.
 * @return Decoded OCSP_SIGNATURE, or NULL on error.
 */
OCSP_SIGNATURE *d2i_OCSP_SIGNATURE(OCSP_SIGNATURE **a, const unsigned char **in, long len);
/**
 * @brief Encode an OCSP Signature to DER.
 * @param a Value to encode.
 * @param out Destination pointer updated like a standard i2d encoder, or NULL to measure length.
 * @return Number of bytes encoded, or a negative value on error.
 */
int i2d_OCSP_SIGNATURE(const OCSP_SIGNATURE *a, unsigned char **out);
/**
 * @brief Return the ASN.1 item descriptor for OCSP_SIGNATURE.
 * @return Pointer to the static ASN1_ITEM for OCSP_SIGNATURE.
 */
const ASN1_ITEM *OCSP_SIGNATURE_it(void);
/**
 * @brief Allocate an empty OCSP TBSRequest (OCSP_REQINFO).
 * @return New OCSP_REQINFO, or NULL on allocation failure.
 */
OCSP_REQINFO *OCSP_REQINFO_new(void);
/**
 * @brief Free a OCSP TBSRequest (OCSP_REQINFO) and its contents.
 * @param a Value to free, or NULL.
 */
void OCSP_REQINFO_free(OCSP_REQINFO *a);
/**
 * @brief Decode a OCSP TBSRequest (OCSP_REQINFO) from DER.
 * @param a Optional destination pointer updated to the result, or NULL.
 * @param in Address of a pointer to the DER input; advanced past the decoded value.
 * @param len Number of bytes available at *@p in.
 * @return Decoded OCSP_REQINFO, or NULL on error.
 */
OCSP_REQINFO *d2i_OCSP_REQINFO(OCSP_REQINFO **a, const unsigned char **in, long len);
/**
 * @brief Encode a OCSP TBSRequest (OCSP_REQINFO) to DER.
 * @param a Value to encode.
 * @param out Destination pointer updated like a standard i2d encoder, or NULL to measure length.
 * @return Number of bytes encoded, or a negative value on error.
 */
int i2d_OCSP_REQINFO(const OCSP_REQINFO *a, unsigned char **out);
/**
 * @brief Return the ASN.1 item descriptor for OCSP_REQINFO.
 * @return Pointer to the static ASN1_ITEM for OCSP_REQINFO.
 */
const ASN1_ITEM *OCSP_REQINFO_it(void);
/**
 * @brief Allocate an empty OCSP CrlID extension value.
 * @return New OCSP_CRLID, or NULL on allocation failure.
 */
OCSP_CRLID *OCSP_CRLID_new(void);
/**
 * @brief Free an OCSP CrlID extension value and its contents.
 * @param a Value to free, or NULL.
 */
void OCSP_CRLID_free(OCSP_CRLID *a);
/**
 * @brief Decode an OCSP CrlID extension value from DER.
 * @param a Optional destination pointer updated to the result, or NULL.
 * @param in Address of a pointer to the DER input; advanced past the decoded value.
 * @param len Number of bytes available at *@p in.
 * @return Decoded OCSP_CRLID, or NULL on error.
 */
OCSP_CRLID *d2i_OCSP_CRLID(OCSP_CRLID **a, const unsigned char **in, long len);
/**
 * @brief Encode an OCSP CrlID extension value to DER.
 * @param a Value to encode.
 * @param out Destination pointer updated like a standard i2d encoder, or NULL to measure length.
 * @return Number of bytes encoded, or a negative value on error.
 */
int i2d_OCSP_CRLID(const OCSP_CRLID *a, unsigned char **out);
/**
 * @brief Return the ASN.1 item descriptor for OCSP_CRLID.
 * @return Pointer to the static ASN1_ITEM for OCSP_CRLID.
 */
const ASN1_ITEM *OCSP_CRLID_it(void);
/**
 * @brief Allocate an empty OCSP ServiceLocator extension value.
 * @return New OCSP_SERVICELOC, or NULL on allocation failure.
 */
OCSP_SERVICELOC *OCSP_SERVICELOC_new(void);
/**
 * @brief Free an OCSP ServiceLocator extension value and its contents.
 * @param a Value to free, or NULL.
 */
void OCSP_SERVICELOC_free(OCSP_SERVICELOC *a);
/**
 * @brief Decode an OCSP ServiceLocator extension value from DER.
 * @param a Optional destination pointer updated to the result, or NULL.
 * @param in Address of a pointer to the DER input; advanced past the decoded value.
 * @param len Number of bytes available at *@p in.
 * @return Decoded OCSP_SERVICELOC, or NULL on error.
 */
OCSP_SERVICELOC *d2i_OCSP_SERVICELOC(OCSP_SERVICELOC **a, const unsigned char **in, long len);
/**
 * @brief Encode an OCSP ServiceLocator extension value to DER.
 * @param a Value to encode.
 * @param out Destination pointer updated like a standard i2d encoder, or NULL to measure length.
 * @return Number of bytes encoded, or a negative value on error.
 */
int i2d_OCSP_SERVICELOC(const OCSP_SERVICELOC *a, unsigned char **out);
/**
 * @brief Return the ASN.1 item descriptor for OCSP_SERVICELOC.
 * @return Pointer to the static ASN1_ITEM for OCSP_SERVICELOC.
 */
const ASN1_ITEM *OCSP_SERVICELOC_it(void);

/**
 * @brief Return a human-readable string for an OCSPResponseStatus code.
 * @param s Status value such as OCSP_RESPONSE_STATUS_SUCCESSFUL.
 * @return Static NUL-terminated description string.
 */
const char *OCSP_response_status_str(long s);
/**
 * @brief Return a human-readable string for an OCSP CertStatus value.
 * @param s Status value such as V_OCSP_CERTSTATUS_GOOD, REVOKED, or UNKNOWN.
 * @return Static NUL-terminated description string (for example "good").
 */
const char *OCSP_cert_status_str(long s);
/**
 * @brief Return a human-readable string for a CRLReason / OCSP revocation reason code.
 * @param s Reason code such as OCSP_REVOKED_STATUS_KEYCOMPROMISE.
 * @return Static NUL-terminated description string.
 */
const char *OCSP_crl_reason_str(long s);

/**
 * @brief Print an OCSP request in human-readable form to a BIO.
 * @param bp Destination BIO.
 * @param a OCSP request to print.
 * @param flags Reserved print flags; pass 0.
 * @return 1 on success, or 0 on failure.
 */
int OCSP_REQUEST_print(BIO *bp, OCSP_REQUEST *a, unsigned long flags);
/**
 * @brief Print an OCSP response in human-readable form to a BIO.
 * @param bp Destination BIO.
 * @param o OCSP response to print.
 * @param flags Reserved print flags; pass 0.
 * @return 1 on success, or 0 on failure.
 */
int OCSP_RESPONSE_print(BIO *bp, OCSP_RESPONSE *o, unsigned long flags);

/**
 * @brief Verify the signature and optional certificate path of a basic OCSP response.
 * @param bs Basic OCSP response to verify.
 * @param certs Optional untrusted certificates used to locate or build the signer path, or NULL.
 * @param st Trusted certificate store used for path validation.
 * @param flags OCSP_* / X509 verify flags controlling verification behaviour.
 * @return 1 on successful verification, or 0 on failure.
 */
int OCSP_basic_verify(OCSP_BASICRESP *bs, STACK_OF(X509) *certs,
    X509_STORE *st, unsigned long flags);

#ifdef __cplusplus
}
#endif
#endif /* !defined(OPENSSL_NO_OCSP) */
#endif
